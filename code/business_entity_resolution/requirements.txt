# Core data processing
polars==1.43.2
pyarrow==21.0.0
numpy==2.2.1

# Stage 1: GPU embedding retrieval
# torch needs a CUDA-enabled wheel for GPU use; install from the matching
# index for your CUDA version, e.g.:
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cu128
# A CPU-only torch install also works (embed.py falls back to CPU
# automatically), just much slower for the ~24M records in this dataset.
torch==2.8.0
sentence-transformers==6.1.0
transformers==5.17.0
tokenizers==0.23.2
huggingface_hub==1.33.0
safetensors==0.8.0
anyascii==0.3.3

# Stage 2: pairwise features + matcher
rapidfuzz==3.14.6
lightgbm==4.7.0
scikit-learn==1.7.2
